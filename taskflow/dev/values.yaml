replicaCount: 1

image:
  repository: ghcr.io/sirvickky001/taskflow
  tag: sha-6369537
  pullPolicy: Always

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: false
  host: ""

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi

env:
  NODE_ENV: development
