apiVersion: v1
kind: Service
metadata:
  name: taskflow
  labels:
    app: taskflow
spec:
  type: {{ .Values.service.type }}
  selector:
    app: taskflow
  ports:
    - name: port-3000
      port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
